import React from 'react';

import { Container } from './styles';

const CreateMaterialModal: React.FC = ({ ...rest }) => {
  return <Container {...rest}></Container>;
};

export default CreateMaterialModal;
