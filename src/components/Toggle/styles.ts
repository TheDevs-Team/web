import styled from 'styled-components';
import Switch from 'react-switch';

export const ToggleStyled = styled(Switch).attrs({
  onColor: '#17c07d',
  uncheckedIcon: false,
  checkedIcon: false,
})``;
